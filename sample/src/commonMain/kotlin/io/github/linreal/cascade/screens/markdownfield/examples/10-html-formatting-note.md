# Formatting imported from another editor

The customer used <u>underline with **nested bold**</u> to call out the account
name. The deadline is
<mark data-cascade-highlight="FFFFD54F">Friday at 17:00 UTC</mark>.

## Follow-up

- Keep the underline when editing with the default HTML bridge
- Keep the highlight color during export
- Compare with **Strict no-HTML**, which must drop HTML-backed formatting and
  report the loss

