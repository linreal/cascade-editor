# Imported status widget

The source system embeds a status fragment below. Test it under each HTML mode
before deciding whether the host should bridge, preserve, strip, or reject it.

<section class="service-status">
  <h2>Document sync</h2>
  <p><strong>Operational</strong> in all regions.</p>
  <p>Last incident: 18 days ago.</p>
</section>

The paragraph after the fragment must remain a separate editable block.

